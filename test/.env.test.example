# =============================================================================
# Slack MCP Server Test Environment Configuration
# =============================================================================
# This file contains environment variables specifically for testing.
# Copy this file to test/.env.test and fill in your actual test values.
#
# This file is used ONLY during test execution and is separate from
# the main production configuration to avoid confusion and security risks.
# =============================================================================

# ─────────────────────────────────────────────────────────────────────────────
# Required: Test Authentication
# ─────────────────────────────────────────────────────────────────────────────
# Test bot token (required for E2E tests)
# Format: xoxb-... (bot token) or xoxp-... (user token)
# Use the same token as SLACK_BOT_TOKEN or a dedicated test token
E2E_TEST_API_TOKEN=xoxb-your-test-slack-bot-token

# ─────────────────────────────────────────────────────────────────────────────
# Optional: Test Channel Configuration
# ─────────────────────────────────────────────────────────────────────────────
# Test channel name (useful for development/testing)
# Format: #channel-name (include the # symbol)
# This channel will be used for test message posting and verification
SLACK_TEST_CHANNEL='#your-test-channel-name'

# Test channel ID (recommended over channel name)
# Format: C1234567890
# Get it from: Right-click channel → View channel details → Copy channel ID
# Using channel ID is more reliable than channel name
SLACK_TEST_CHANNEL_ID=C1234567890

# ─────────────────────────────────────────────────────────────────────────────
# Optional: Test Environment Control
# ─────────────────────────────────────────────────────────────────────────────
# Disable loading of main .env file during tests (recommended)
# Set to "true" to ensure tests use only test-specific configuration
MCP_NO_ENV_FILE=true

# ─────────────────────────────────────────────────────────────────────────────
# Notes
# ─────────────────────────────────────────────────────────────────────────────
# 1. This file is only loaded during test execution
# 2. Values here override main .env file when MCP_NO_ENV_FILE=false
# 3. Never commit this file with real credentials to version control
# 4. Add test/.env.test to your .gitignore file
# 5. Set appropriate file permissions: chmod 600 test/.env.test
#
# For more information, see:
# https://chisanan232.github.io/slack-mcp-server/document/server-references/environment-configuration
# =============================================================================
